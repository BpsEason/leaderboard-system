# leaderboard-system/mysql-master/my.cnf
[mysqld]
server-id=1
log_bin=mysql-bin
binlog_do_db=leaderboard_db # Only replicate changes for this database
# If using GTID: gtid_mode=ON, enforce_gtid_consistency=ON
# max_connections=500 # Example
# innodb_buffer_pool_size=1G # Example